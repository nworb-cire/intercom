[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "intercom"
version = "0.1.0"
description = "Application-controlled FreeSWITCH intercom core and integrations"
requires-python = ">=3.12"
dependencies = []

[tool.setuptools.packages.find]
include = ["core*", "intercom_integrations*"]
